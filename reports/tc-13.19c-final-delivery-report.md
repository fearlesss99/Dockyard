# TC-13.19c · 最终交付报告

## 仓库与基线

| 项目 | 值 |
|---|---|
| Repository | `AgentDeskSkill-0.1.0-beta` |
| Branch | `feat/mad-agentdesk-integration` |
| Parent SHA（上一基线） | `2e47f5aea472f1dfc95fe791edc29d4fd06ff3c1`（TC-13.19b 报告提交） |
| Implementation Commit SHA | `ce3530c3999100a7660371db0b8c24084be31616` |
| Final HEAD SHA | `ce3530c3999100a7660371db0b8c24084be31616` |
| 测试文件 SHA-256 | `beded1933dd8e2b355454979fce60f92c606b3877fb4ab59abcefcc0dd420106` |

## 修改文件

仅新增一个文件；未修改任何生产代码或既有测试：

- `tests/test_workflow_escalation_e2e.py`（新增 1665 行）

## Required Checks — 可复核输出

### Check 1: 定向升级重派 E2E 测试

```
$ python -m unittest tests.test_workflow_escalation_e2e.WorkflowEscalationE2ETests.test_blocked_audit_escalates_and_redispatches_to_integration -v
test_blocked_audit_escalates_and_redispatches_to_integration ... ok

----------------------------------------------------------------------
Ran 1 test in 3.502s
OK
```

运行次数：5 次（开发迭代 + 最终验证）。

### Check 2: `git status --short`

```
（无输出 — 工作区完全干净）
```

## 完整生产调用链

### Attempt 1 — Basic Worker 被阻塞

1. `WorkflowOrchestrator.run_dispatch_cycle()`（`worker_kind=WorkerKind.BASIC_AGENT`）：
   - `StateProvider.snapshot()` → `acquire_worker_slot(basic_agent-N)`
   - `apply_transition(TASK_DISPATCHED)` with approval_gate → `ready → dispatched`
   - Heartbeat + `run_worker_observed()` → `asyncio.create_subprocess_exec(fake-claude.exe)` → `FakeProcess(pid=10001)`
   - `_AckObserver.on_dispatch_started()` → `apply_transition(DISPATCH_ACKNOWLEDGED)` → `dispatched → in_progress`
   - `decode_worker_result()` → `require_delivery_receipt()` → `apply_transition(DELIVERY_SUBMITTED)` → `in_progress → review_ready`
   - Heartbeat cancel → `release_worker_slot()`

2. `WorkflowOrchestrator.run_acceptance_cycle()`：
   - `run_audit_gateway()` → `asyncio.create_subprocess_exec(fake-mad.exe)` → `FakeProcess(pid=10002)` → `MadAuditGatewayResult(verdict=blocked, issues=[BLOCK-001])`
   - `verdict=blocked` → early return `accept_transition=None, integrate_transition=None`

3. `WorkflowOrchestrator.run_blocked_audit_cycle()`（TC-13.18d.2）：
   - `evaluate_escalation(EscalationRequest(current_worker_kind=BASIC_AGENT))` — 恰好执行一次
   - 真实 EscalationService 返回 `EscalationDecision(action=ESCALATE, current=Basic, next=Standard)`
   - `apply_transition(TASK_BLOCKED)` with `lease=None`（PM-only 转换）→ `review_ready → blocked`
   - 未调用 MAD、lease 或 dispatch

### Attempt 2 — Standard Worker 重派成功

4. `WorkflowOrchestrator.run_escalated_redispatch()`（TC-13.18d.3）：
   - 验证 EscalationDecision（`action=ESCALATE`, `next_worker_kind=STANDARD_AGENT`）
   - 验证 BlockedPayload（`blocked_attempt_valid=False`, `resume_state="ready"`）
   - 验证 next DispatchCycleRequest（`attempt=2`, 新 `dispatch_id`, `worker_kind=STANDARD_AGENT`）
   - `apply_transition(BLOCKER_RESOLVED)` with `lease=None` → `blocked → ready`
   - `run_dispatch_cycle(dispatch_cycle_req_a2, providers)` — 完整真实 dispatch

5. `run_acceptance_cycle()`（attempt 2）：
   - `run_audit_gateway()` → `FakeProcess(pid=10004)` → `MadAuditGatewayResult(verdict=pass)`
   - `apply_transition(DELIVERY_ACCEPTED)` under review lease → `review_ready → accepted`
   - `apply_transition(CHANGE_INTEGRATED)` with `lease=None` → `accepted → integrated`

## Basic → Standard 升级证据

`EscalationService.evaluate_escalation()`（真实生产代码）的逐项证据：

- 输入：`EscalationRequest(current_worker_kind=WorkerKind.BASIC_AGENT)`
- `_PROGRESSION[BASIC_AGENT]` → `WorkerKind.STANDARD_AGENT`
- `next_kind is not None` → `action = EscalationAction.ESCALATE`
- 输出：`EscalationDecision(action=ESCALATE, current_worker_kind=BASIC_AGENT, next_worker_kind=STANDARD_AGENT)`

升级决策由真实 `escalation_service.py` 产生，未 mock。

## Mock 边界

| 组件 | Mock 方式 | 未 Mock |
|---|---|---|
| `asyncio.create_subprocess_exec` | 单一路由函数，按顺序分发 4 个 FakeProcess | — |
| 时钟 | FakeClock（UTC，1s tick） | — |
| `WorkflowOrchestrator` | — | ✓ |
| `EscalationService.evaluate_escalation` | — | ✓ |
| `ControlPlaneTransitionService.apply_transition` | — | ✓ |
| `ApprovalGate.check/require` | — | ✓ |
| `StateProvider.snapshot` | — | ✓ |
| `decode_worker_result` / `require_delivery_receipt` | — | ✓ |
| `acquire_worker_slot` / `release_worker_slot` | — | ✓ |
| `run_audit_gateway` | — | ✓ |
| `run_worker_observed` | — | ✓ |

## Identity 对照

| 属性 | Attempt 1（Basic） | Attempt 2（Standard） |
|---|---|---|
| `dispatch_id` | `DSP-ESCALATION-001` | `DSP-ESCALATION-002` |
| `attempt` | `1` | `2` |
| `worker_kind` | `BASIC_AGENT` | `STANDARD_AGENT` |
| `outbox_message_id` | `MSG-ESCALATION-OUTBOX-A1` | `MSG-ESCALATION-OUTBOX-A2` |
| `implementation_commit` | A1 SHA | A2 SHA（不同） |
| `report_commit` | A1 报告 SHA | A2 报告 SHA（不同） |
| `dispatch event_id` | `EVT-DISPATCH-ESCALATION-A1` | `EVT-DISPATCH-ESCALATION-A2` |
| `block event_id` | `EVT-BLOCK-ESCALATION-A1` | N/A |
| `resolve event_id` | `EVT-RESOLVE-ESCALATION-A1` | N/A |
| `accept event_id` | `EVT-ACCEPT-ESCALATION-A1`（未使用） | `EVT-ACCEPT-ESCALATION-A2` |
| `integrate event_id` | `EVT-INTEGRATE-ESCALATION-A1`（未使用） | `EVT-INTEGRATE-ESCALATION-A2` |

## 事件与 Guard 顺序

按 `occurred_at` 排序：

| # | 事件类型 | Attempt | Worker | approval_gate |
|---|---|---|---|---|
| 1 | `TASK_DISPATCHED` | 1 | Basic | ✓ |
| 2 | `DISPATCH_ACKNOWLEDGED` | 1 | — | — |
| 3 | `DELIVERY_SUBMITTED` | 1 | — | — |
| 4 | `TASK_BLOCKED` | — | — (PM-only) | — |
| 5 | `BLOCKER_RESOLVED` | — | — (PM-only) | — |
| 6 | `TASK_DISPATCHED` | 2 | Standard | ✓ |
| 7 | `DISPATCH_ACKNOWLEDGED` | 2 | — | — |
| 8 | `DELIVERY_SUBMITTED` | 2 | — | — |
| 9 | `DELIVERY_ACCEPTED` | 2 | — | ✓ |
| 10 | `CHANGE_INTEGRATED` | — | — | ✓ |

已验证不存在：
- `DELIVERY_RETURNED`
- `TASK_REQUEUED`
- Attempt 1 的 `DELIVERY_ACCEPTED` 或 `CHANGE_INTEGRATED`
- 第三次 dispatch
- Standard 以外的 next worker kind
- 自动连续升级到 Advanced

## Subprocess 调用

| # | 顺序 | 可执行文件 | 用途 |
|---|---|---|---|
| 1 | Claude A1 | fake-claude.exe | Basic Worker 交付 — exit 0, completed |
| 2 | MAD blocked | fake-mad.exe | 审计 — verdict=blocked, BLOCK-001 |
| 3 | Claude A2 | fake-claude.exe | Standard Worker 交付 — exit 0, completed |
| 4 | MAD pass | fake-mad.exe | 审计 — verdict=pass |

- 总 subprocess 调用：恰好 4 次
- 所有调用均使用 argv tuple，不通过 shell 字符串

## 清理结果

| 检查项 | 结果 |
|---|---|
| Worker Slot Lease 剩余 | 0（全部释放） |
| `.tmp-*` 文件 | 0 |
| Git remote | 0 |
| cwd 改变 | 无 |
| Asyncio task 泄漏 | ≤3 tasks |
| 源仓库 | 未修改 |

## 未运行测试（按卡要求）

- 未运行完整 pytest、discover、release smoke 或 `python -O`
- 未运行 TC-13.20 Dashboard 材料
- Happy-path E2E（TC-13.19a）和 recovery E2E（TC-13.19b）未重新运行 — 本卡禁止修改其路径

## 接受标准 — 全部通过

- [x] 仅新增 `tests/test_workflow_escalation_e2e.py`
- [x] Basic → Standard 升级决策由真实 EscalationService 产生
- [x] `TASK_BLOCKED` 在 `BLOCKER_RESOLVED` 之前
- [x] `BLOCKER_RESOLVED` 在第二次 dispatch 之前
- [x] Attempt 严格从 1 增长到 2
- [x] 两次 dispatch identity 完全独立
- [x] 第一次 blocked 不产生 acceptance 或 integration
- [x] 第二次 pass 后任务进入 `integrated`
- [x] Acceptance 绑定 attempt 2 的 dispatch identity
- [x] `StateProvider.snapshot()` 读取最终一致状态
- [x] 最终 `current_dispatch is None`
- [x] 所有 Worker Slot Lease 均已释放
- [x] 无临时文件、Git remote、cwd 变化或 asyncio task 泄漏
- [x] 四次 subprocess 调用的次数和顺序正确
- [x] 指定单条 E2E 通过
- [x] 未运行完整测试套件
- [x] 所有 required checks 有可复核证据
- [x] 最终只生成一份报告
- [x] 最终 `git status --short` 无输出
