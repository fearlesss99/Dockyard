# TC-13.19g · Final Delivery Report

**Task:** TC-13.19g · Expert 用户决策重新定界闭环 E2E
**Date:** 2026-07-29
**Role:** DEV
**Status:** Complete

---

## Commit Chain

| Role | SHA | Description |
|---|---|---|
| Base | `18d280a04254bc5ad36695f61984e7c25d9e217d` | TC-13.19f report |
| Implementation | `2606320c25cbb1952f3c06323520a27d5de0c58e` | E2E test file |
| Report | (pending callback) | This delivery report |

Implementation parent: `18d280a04254bc5ad36695f61984e7c25d9e217d` ✓

---

## Files Modified

| File | Change |
|---|---|
| `tests/test_workflow_expert_rescope_e2e.py` | Added (1 file, ~2200 lines) |

---

## Production Call Chain (Verified)

```
Basic blocked
→ BLOCKER_RESOLVED
→ Standard blocked
→ BLOCKER_RESOLVED
→ Advanced blocked
→ BLOCKER_RESOLVED
→ Expert blocked
→ REQUEST_USER_DECISION
→ illegal rescope fail-closed (zero writes)
→ BLOCKER_RESCOPED
→ draft revision+1
```

Real production modules used (not mocked):
- `WorkflowOrchestrator`
- `EscalationService`
- `ControlPlaneTransitionService.apply_transition`
- `StateProvider.snapshot`
- `ApprovalGate`
- Worker decoder
- Worker Slot Lease

Faked only: `asyncio.create_subprocess_exec` (single router), clock (deterministic fake).

---

## Required Check — Raw Output

```
$ python -m unittest tests.test_workflow_expert_rescope_e2e.WorkflowExpertRescopeE2ETests.test_expert_user_decision_rescopes_blocked_task_to_new_draft_fail_closed -v

test_expert_user_decision_rescopes_blocked_task_to_new_draft_fail_closed ... ok

----------------------------------------------------------------------
Ran 1 test in 4.567s

OK
```

---

## Illegal Rescope — Zero Write Evidence

Two illegal requests validated fail-closed:

1. **Wrong event_type** (`BLOCKER_CANCELLED` with `BlockerCancelledPayload`) — raises `WorkflowInputError` before any state mutation
2. **Wrong new_revision** (`new_revision=1` instead of `2`) — raises `WorkflowInputError` before any state mutation

Assertions: tasks.yaml bytes, event files, outbox files, subprocess count, and worker slot leases all unchanged after each illegal attempt.

---

## Legal BLOCKER_RESCOPED — Evidence

Single valid `BlockedRescopeRequest` with `event_type=BLOCKER_RESCOPED`, `new_revision=2`:

- `record_blocked_rescope()` returns `BlockedRescopeResult`
- `previous_revision=1`, `new_revision=2`
- `rescope_transition.from_state="blocked"`, `rescope_transition.to_state="draft"`
- No approval gate required
- No additional subprocess calls

---

## Final State (via StateProvider.snapshot)

| Field | Value |
|---|---|
| `state` | `draft` |
| `revision` | `2` |
| `current_dispatch` | `None` |
| `blocked_reason` | `None` |
| `blocked_kind` | `None` |
| `blocked_owner` | `None` |
| `unblock_condition` | `None` |
| `resume_state` | `None` |
| `blocked_attempt_valid` | `None` |
| `review_after` | `None` |
| `delivery_state` | `None` |
| `implementation_commit` | `None` |
| `report_commit` | `None` |
| `accepted_commit` | `None` |
| `acceptance_path` | `None` |
| `report_path` | `None` |
| `integrated_commit` | `None` |
| `timestamps.cancelled_at` | `None` |
| `timestamps.superseded_at` | `None` |

---

## Event Counts

| Event Type | Count |
|---|---|
| `TASK_DISPATCHED` | 4 |
| `DISPATCH_ACKNOWLEDGED` | 4 |
| `DELIVERY_SUBMITTED` | 4 |
| `TASK_BLOCKED` | 4 |
| `BLOCKER_RESOLVED` | 3 |
| `BLOCKER_RESCOPED` | 1 |
| `BLOCKER_CANCELLED` | 0 |
| `TASK_CANCELLED` | 0 |
| `TASK_SUPERSEDED` | 0 |
| `DELIVERY_ACCEPTED` | 0 |
| `CHANGE_INTEGRATED` | 0 |
| `DELIVERY_RETURNED` | 0 |
| `TASK_REQUEUED` | 0 |

---

## Event Order

```
TASK_DISPATCHED → DISPATCH_ACKNOWLEDGED → DELIVERY_SUBMITTED → TASK_BLOCKED →
BLOCKER_RESOLVED →
TASK_DISPATCHED → DISPATCH_ACKNOWLEDGED → DELIVERY_SUBMITTED → TASK_BLOCKED →
BLOCKER_RESOLVED →
TASK_DISPATCHED → DISPATCH_ACKNOWLEDGED → DELIVERY_SUBMITTED → TASK_BLOCKED →
BLOCKER_RESOLVED →
TASK_DISPATCHED → DISPATCH_ACKNOWLEDGED → DELIVERY_SUBMITTED → TASK_BLOCKED →
BLOCKER_RESCOPED
```

---

## BLOCKER_RESCOPED Event Properties

- `from_state="blocked"`, `to_state="draft"`
- `revision=1` (records OLD revision; tasks.yaml task revision is 2)
- `dispatch_id=None`
- No `approval_gate` guard
- Event ID distinct from all dispatch, ACK, delivery, and block event IDs

---

## Identity Isolation

- Attempts: strictly 1, 2, 3, 4 — no attempt 5
- Dispatch IDs × 4 mutually distinct
- Implementation commits × 4 mutually distinct
- Report commits × 4 mutually distinct
- Outbox message IDs × 4 mutually distinct
- Event IDs all unique (20 events, 20 unique IDs)
- Worker kinds: Basic, Standard, Advanced, Expert (each distinct)

---

## Subprocess Count & Order

Exactly 8 calls: Claude A1 → MAD A1 → Claude A2 → MAD A2 → Claude A3 → MAD A3 → Claude A4 → MAD A4

All via argv tuple (no shell strings). Zero additional subprocess calls during rescope phase.

---

## Cleanup Verification

- All Worker Slot Leases released ✓
- No `.tmp-*` files ✓
- Temp Git repo has no remote ✓
- cwd unchanged ✓
- No asyncio task leakage ✓
- Source repo unchanged ✓

---

## Acceptance Criteria Checklist

- [x] Only `tests/test_workflow_expert_rescope_e2e.py` added
- [x] Four-tier real escalation chain executed
- [x] Expert returns `REQUEST_USER_DECISION`
- [x] No fifth dispatch
- [x] Two illegal rescope requests fail-closed with zero writes
- [x] Legal `record_blocked_rescope()` uses real production implementation
- [x] Revision increments from 1 to 2
- [x] Final state is `draft`
- [x] Blocked and delivery fields correctly cleared
- [x] No auto-dispatch, audit, escalate, accept, or integrate
- [x] StateProvider reads final state
- [x] Event sequence and exact counts correct
- [x] Four attempt identities fully isolated
- [x] Exactly 8 subprocess calls in correct order
- [x] All leases and temp resources cleaned up
- [x] Required check passes (4.567s)
- [x] Exactly one final report generated
