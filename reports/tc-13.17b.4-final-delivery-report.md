# TC-13.17b.4 · Final Delivery Report

**Task:** TC-13.17b.4 · 收紧终态时间戳非空校验
**Date:** 2026-07-29
**Role:** DEV
**Status:** Complete

---

## Commit Chain

| Role | SHA | Description |
|---|---|---|
| Base | `0c568c975720d1cd0e5fc72985453cd7c204ccf9` | TC-13.17b.3 base |
| Implementation | `1b8b948f12501529cba29db6c2ee319fb4f24919` | Tighten `_opt_terminal()` empty-string validation |
| Report | `REPORT_SHA_PLACEHOLDER` | This delivery report |

Implementation parent: `0c568c975720d1cd0e5fc72985453cd7c204ccf9` ✓

---

## Files Modified

| File | Change |
|---|---|
| `skills/agentdesk/scripts/state_provider.py` | `_opt_terminal()` now rejects empty string (line 1256) |
| `tests/test_state_provider.py` | Expanded `test_terminal_timestamp_types_remain_fail_closed` with empty `cancelled_at` and empty `superseded_at` assertions |
| `reports/tc-13.17b.4-final-delivery-report.md` | This file (report commit only) |

---

## Fix: Empty String Rejection

### Before (TC-13.17b.3)

```python
def _opt_terminal(key: str) -> str | None:
    if key not in raw:
        return None
    v = raw[key]
    if v is None:
        return None
    if not isinstance(v, str):
        raise StateProviderSchemaError("timestamp must be str or null")
    return _freeze(v)  # ← accepted "" silently
```

In-memory input `cancelled_at: ""` was accepted and returned `""`.

### After (TC-13.17b.4)

```python
def _opt_terminal(key: str) -> str | None:
    if key not in raw:
        return None
    v = raw[key]
    if v is None:
        return None
    if not isinstance(v, str):
        raise StateProviderSchemaError("timestamp must be str or null")
    if not v:                                                    # ← NEW
        raise StateProviderSchemaError("timestamp must be str or null")  # ← NEW
    return _freeze(v)
```

Empty string now raises `StateProviderSchemaError`, matching the frozen contract:

> When present, both must be a non-empty string or the JSON null value.

---

## Required Checks — Results

### Check 1: StateProvider tests

```
$ python -m unittest tests.test_state_provider.TC1317bProductionTests.test_snapshot_accepts_cancelled_at_and_defaults_superseded_at tests.test_state_provider.TC1317bProductionTests.test_snapshot_accepts_superseded_at_and_defaults_cancelled_at tests.test_state_provider.TC1317bProductionTests.test_terminal_timestamp_types_remain_fail_closed -v

test_snapshot_accepts_cancelled_at_and_defaults_superseded_at ... ok
test_snapshot_accepts_superseded_at_and_defaults_cancelled_at ... ok
test_terminal_timestamp_types_remain_fail_closed ... ok

----------------------------------------------------------------------
Ran 3 tests in 0.110s

OK
```

### Check 2: E2E cancellation test

```
$ python -m unittest tests.test_workflow_expert_cancellation_e2e.WorkflowExpertCancellationE2ETests.test_expert_user_decision_cancels_blocked_task_fail_closed -v

test_expert_user_decision_cancels_blocked_task_fail_closed ... ok

----------------------------------------------------------------------
Ran 1 test in 4.598s

OK
```

---

## Protected File Integrity

| File | SHA-256 |
|---|---|
| `tests/test_workflow_expert_cancellation_e2e.py` | `7DB426E749AB4AFBC5533BC8798B64628ED806C785E040B8DE6B2F1110449A60` |

SHA-256 unchanged from pre-task baseline. ✓

---

## Final Working Tree State

```
 M skills/agentdesk/scripts/state_provider.py
 M tests/test_state_provider.py
?? tests/test_workflow_expert_cancellation_e2e.py
```

Only the two implementation files are modified (tracked in the implementation commit).
Only pre-existing untracked file: `tests/test_workflow_expert_cancellation_e2e.py`.

---

## TC-13.19f Declaration

TC-13.19f file (`tests/test_workflow_expert_cancellation_e2e.py`) was **not modified, staged, or committed** during this task.
Its SHA-256 remains `7DB426E749AB4AFBC5533BC8798B64628ED806C785E040B8DE6B2F1110449A60`.
It remains an untracked file in the working tree exactly as it was at task start.

---

## Acceptance Criteria Checklist

- [x] Repository, branch, HEAD, and working tree correct
- [x] Empty `cancelled_at` fail-closed (raises `StateProviderSchemaError`)
- [x] Empty `superseded_at` fail-closed (raises `StateProviderSchemaError`)
- [x] Absent, `null`, and valid non-empty string values remain compatible
- [x] Existing non-string and unknown-key tests still passing
- [x] TC-13.19f single real E2E still passing
- [x] Public contract not modified
- [x] No other production modules modified
- [x] Protected file content and SHA-256 unchanged
- [x] Final working tree has only pre-existing untracked TC-13.19f file
- [x] All required checks executed with raw output captured
- [x] Exactly one delivery report generated
