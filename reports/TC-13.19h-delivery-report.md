# TC-13.19h — Quiescent Task Cancellation E2E — Delivery Report

## Identifiers

- **Parent SHA**: `3617fdfe6a60e41dc211b775d7dbbeaa1faabad3`
- **Test SHA**: `3aa471dc5f9c4f4a0dde08a9c488f071026aa67d`
- **Report SHA**: _(committed below)_

## Unique Modified File

- `tests/test_workflow_quiescent_cancellation_e2e.py` (new, 993 lines)

No production code, ADR, or existing tests were modified.

## Test Results

```text
python -m unittest tests.test_workflow_quiescent_cancellation_e2e -v
```

```
Ran 3 tests in 1.941s — OK

  test_quiescent_cancellation_happy_path ................ ok
  test_active_dispatch_refuses_quiescent_cancellation ... ok
  test_cas_mismatch_rejected_before_transition .......... ok
```

## Final Task / Event State

### Happy Path (test_quiescent_cancellation_happy_path)

- **canonical task**: `state = "cancelled"`, `current_dispatch = None`, `revision = 1` (unchanged per TASK_CANCELLED frozen contract)
- **cancelled_at**: written to `timestamps.cancelled_at`
- **event**: exactly 1 `EVT-QUIESCENT-CANCEL-HAPPY` of type `TASK_CANCELLED`
  - `from_state = "ready"`
  - `to_state = "cancelled"`
  - `dispatch_id = None`, `attempt = None`
- **final StateProvider.snapshot()**: successful, reads cancelled task

## Fail-Closed Byte-Exact Evidence

### 1. Active Dispatch Refused

- **Precondition**: task in `dispatched` state with active `current_dispatch`
- **Action**: `cancel_quiescent_task()` → `WorkflowInputError` ("task has active dispatch — quiescent cancellation refused")
- **tasks.yaml**: byte-for-byte unchanged
- **events/**: no new files
- **outbox/**: no new files
- **task state**: still `dispatched`

### 2. CAS Mismatch Rejected

- **Sub-case A**: `expected_revision = 99` (actual `revision = 3`) → `WorkflowInputError` ("task revision does not match CAS expected_revision")
- **Sub-case B**: `expected_state = "blocked"` (actual `state = "ready"`) → `WorkflowInputError` ("task state does not match CAS expected_state")
- **tasks.yaml**: byte-for-byte unchanged after both attempts
- **events/**: unchanged
- **Zero new events**: confirmed via StateProvider snapshot
- **Task state**: still `ready`, `revision = 3`

## Zero External Calls Proof

- No `asyncio.create_subprocess_exec` mock required (no subprocess calls)
- No model/API/network calls — `cancel_quiescent_task()` path: snapshot → CAS validate → `apply_transition(TASK_CANCELLED, lease=None)`
- PM-only transition path (`TASK_CANCELLED`) uses `needs_worker_lease=False`, `produces_outbox=False`, `produces_acceptance=False`
- `lease=None` — no `WorkerSlotLease` operations (confirmed: 0 leases in slot store)
- No temporary file residue (`.tmp-*` glob empty)
- No Git remote

## git diff --check

```text
<empty — clean>
```

## Clean Working Tree

```text
<empty — nothing to commit, working tree clean>
```

## Transition to TC-13.19i

TC-13.19h is complete. TC-13.19i (quiescent supersession E2E) begins.
