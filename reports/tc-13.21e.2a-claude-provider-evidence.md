# TC-13.21e.2a.1 — Claude Provider Evidence Semantics Correction

**Status: Verified — TC-13.21e.2a.1 (evidence semantics corrected)**
**Date: 2026-08-01**

## Abstract

Corrects the TC-13.21e.2a evidence report. The previous report claimed
"total model calls = 1 (exactly one)" and asserted model identity beyond
what the evidence can prove. This report distinguishes, strictly:

- CLI process invocation count (observed)
- Claude Code conversation turns (`num_turns`, observed)
- transport attempts (observed)
- model identifiers appearing in `modelUsage` (observed)
- AgentDesk automatic retry (observed)
- provider backend HTTP request count (NOT provable from CLI output)
- underlying vendor base model behind a proxy/model map (NOT provable)

A single real Claude CLI call was executed with fixed Unicode and
multiline proof strings (`验证通过`, `✓`, `第一行`, `第二行`), captured
through the production AgentDesk path: `ClaudeCodeProvider.build_invocation`
→ `run_dispatch` → `decode_worker_result` → `require_delivery_receipt`.

No production code was modified. No tools were used by the evidence call.
No raw model output, API keys, or absolute workspace paths are stored in
this report — only boolean results and length counts.

## Real Claude CLI Invocation

One Claude CLI process was launched via `run_dispatch` (one
`create_subprocess_exec`); it exited 0 after one conversation turn.

| Field | Value |
|-------|-------|
| CLI | `claude` (Claude Code v2.1.214) |
| CLI process invocation count | **1** (one `create_subprocess_exec`) |
| Claude Code `num_turns` | **1** |
| AgentDesk automatic retry | **0** (single attempt, no retry path invoked) |
| Requested model identifier (`--model`) | `claude-sonnet-5` |
| `modelUsage` identifiers reported | `claude-haiku-4-5`, `claude-sonnet-5` |
| Provider HTTP request count | **Unverified** — not provable from CLI stdout |
| Underlying vendor base model | **Unverified** — not provable through proxy/model map |
| Permission mode | `plan` |
| Effort | `low` (mapped from `efficient`) |
| Exit code | 0 |
| Invocation duration | ~5.4s |
| stdout raw bytes | 1614 |
| stdout UTF-8 characters | 1592 |
| stderr raw bytes | 0 |
| Workspace | Temporary directory (deleted) |

## Unicode / Multiline Transport Evidence

The single real call returned all required proof strings in the worker
`result` field (raw stdout was never saved — only boolean results and
lengths are recorded):

| Proof string | Present |
|--------------|---------|
| `验证通过` (Chinese) | yes |
| `✓` (Unicode check) | yes |
| `第一行` (line 1) | yes |
| `第二行` (line 2) | yes |

| Check | Result |
|-------|--------|
| Line order preserved (`第一行` before `第二行`) | yes |
| stdout raw byte count | 1614 |
| UTF-8 strict decode success | yes |
| UTF-8 decoded character count | 1592 |
| Byte count vs character count | recorded separately, never conflated |

## Decoder / Envelope / Receipt

| Field | Value |
|-------|-------|
| `decode_worker_result(WorkerResult, "2.1.214")` | SUCCESS |
| Provider | `claude` |
| Model ID | `claude-sonnet-5` |
| Status | `completed` |
| `implementation_commit` | `a053e5c90610c8c4b193c87380225cd56769fb8d` |
| `report_commit` | `7e1cb229517946cbcaf3297f8ce880e02e25fb4a` |
| `require_delivery_receipt(WorkerOutput)` | SUCCESS |
| Receipt stdout SHA-256 | `b6c402565122d09447e1b1db1766d91c0b2aaca117c8fcf3f35552558956751a` |
| Wrapper JSON valid | yes |
| Wrapper `type` / `subtype` | `result` / `success` |
| Wrapper `stop_reason` | `end_turn` |
| Wrapper `terminal_reason` | `completed` |
| Envelope `schema_version` | `agentdesk.worker-output/v1` |
| Envelope identity (task/revision/attempt/dispatch) | matched |
| Envelope `summary` decoded | `第一行` / `第二行` / `验证通过 ✓` |

## Model Identity Boundaries

- Claimed: the Claude Code CLI request carried model identifier
  `claude-sonnet-5` (`--model claude-sonnet-5`).
- Claimed: CLI output `modelUsage` reported identifiers
  `claude-haiku-4-5` and `claude-sonnet-5`.
- NOT claimed: the underlying base model vendor or model was
  cryptographically verified, or that a specific base model answered.
- NOT claimed: model identity per self-description.
- Backend HTTP request count and any proxy/model-map routing are
  **Unverified** — the CLI output cannot prove them.

## Cleanup

- stdout / stderr capture: in-memory only, never written to disk
- Temporary workspace directory: deleted (context-managed)
- Evidence JSON: written to OS temp (outside repo), deleted after use
- Temporary evidence runner script: deleted after use (not committed)
- No raw model output committed to repository
- No API keys, tokens, or absolute paths in this report

## Status Advancement

| Interface | Previous | New |
|-----------|----------|-----|
| Claude CLI execution | Verified — TC-13.21e.2a | **Verified** |
| Claude output decoder/receipt | Verified | **Verified** |
| Unicode/multiline transport | Target | **Verified** |
| Requested model identifier | Observed | **Observed** |
| Underlying proxy model identity | — | **Unverified** |
| Provider HTTP request count | — | **Unverified** |
| Codex runtime | Target/deferred | **Target/deferred** |
| Provider 429 detection | Target — TC-13.14c | **Target** |

## No Secret Leakage Proof

This report contains:

- No API keys, tokens, or passwords
- No absolute workspace paths
- No raw model output
- No credential-like field values
- No provider endpoint URLs
- No environment variable values

## Test Not Run

- Full release smoke (905 tests)
- unittest discover
- pytest full project
- python -O
- Previously passing Doctor production tests
- Real CLI, model, API, or network tests beyond the single evidence call

Only the single real Claude call was executed for evidence collection.
