# TC-13.22a.2 — TaskDifficulty 契约测试空白匹配验证

## 基线

- Repository: `C:\Users\lc\dev\AgentDeskSkill-0.1.0-beta`
- Branch: `feat/mad-agentdesk-integration`
- Parent SHA: `b10adae76dd39e21d5d89ff1b4b1233b10e70aa2`

## 修改

仅修改 `tests/test_release_smoke.py` 中的两个指定测试 matcher：

- `test_difficulty_override_is_a_separate_authorization_domain`
- `test_evidence_canonical_replay_ancestry_and_security_rules`

两个测试均在局部将契约文本空白规范化为单个空格，再继续匹配完整短语。未删除断言，未使用宽泛匹配，未修改共享 helper。

未修改：

- `task-difficulty-assessment-contract.md`
- ADR
- 任何生产代码
- 其他测试类

## 验证

精确测试类：

`TC1322aTaskDifficultyAssessmentContractFreezeTests`

执行命令：

`C:\Users\lc\anaconda3\python.exe -m unittest tests.test_release_smoke.TC1322aTaskDifficultyAssessmentContractFreezeTests -v`

结果：

```text
Ran 17 tests in 0.003s
OK
```

Contract: `Current — TC-13.22a`

Runtime / deterministic assessor: `Target — TC-13.22b`

Interface #22：状态不变。

未运行完整 Release Smoke、pytest、discover 或 `python -O`。未调用模型、API、网络或 provider CLI。

## 提交

本报告不记录自身提交 SHA。
