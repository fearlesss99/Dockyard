# TC-13.19b · 最终交付报告

## 仓库与基线

| 项目 | 值 |
|---|---|
| Repository | `AgentDeskSkill-0.1.0-beta` |
| Branch | `feat/mad-agentdesk-integration` |
| Parent SHA (基线) | `41dfa9f61ef5ddd05f6e53d72c7d0573d9b38961` |
| Implementation Commit SHA | `750cd9671c995f756a9c49cbca437ae0ce4c1e58` |
| Final HEAD SHA | `750cd9671c995f756a9c49cbca437ae0ce4c1e58` |
| 测试文件 SHA-256 | `9d8155028eb4825041b078ecbe453e8c43dd41e2cc7340764511245acb4c378a` |

## 修改文件

仅新增一个文件；未修改任何生产代码：

- `tests/test_workflow_recovery_e2e.py`（新增 2042 行）

## Required Checks — 可复核输出

### Check 1: 定向恢复 E2E 测试

```
$ python -m unittest tests.test_workflow_recovery_e2e.WorkflowRecoveryE2ETests.test_failed_audit_requeues_and_second_attempt_integrates -v
test_failed_audit_requeues_and_second_attempt_integrates (tests.test_workflow_recovery_e2e.WorkflowRecoveryE2ETests.test_failed_audit_requeues_and_second_attempt_integrates) ... ok

----------------------------------------------------------------------
Ran 1 test in 3.375s
OK
```

运行次数：6 次（开发迭代 + 最终验证）。

### Check 2: Delivery-Remediation 定向回归

```
$ python -m unittest tests.test_workflow_orchestrator.WorkflowOrchestratorDeliveryRemediationTests -v
Ran 30 tests in 9.280s
OK
```

30/30 全部通过。

### Check 3: `git status --short`

```
（无输出 — 工作区完全干净）
```

## 完整生产调用链

### Attempt 1 — 交付失败并返工

1. `WorkflowOrchestrator.run_dispatch_cycle()`：
   - `StateProvider.snapshot()`
   - `acquire_worker_slot()` → WorkerSlotLease
   - `ControlPlaneTransitionService.apply_transition(TASK_DISPATCHED)` with approval_gate → `ready → dispatched`
   - Heartbeat start → `renew_worker_slot()` loop
   - `run_worker_observed()` → `asyncio.create_subprocess_exec(fake-claude.exe)` → FakeProcess → `WorkerResult(exit_code=0)`
   - `_AckObserver.on_dispatch_started()` → `apply_transition(DISPATCH_ACKNOWLEDGED)` with DispatchCAS → `dispatched → in_progress`
   - `decode_worker_result()` → `WorkerOutput(status=completed)`
   - `require_delivery_receipt()` → `DeliveryReceipt(identity=(TC-001, r1, a1, DSP-E2E-RECOV-001))`
   - `apply_transition(DELIVERY_SUBMITTED)` with DispatchCAS → `in_progress → review_ready`
   - Heartbeat cancel → `release_worker_slot()`

2. `WorkflowOrchestrator.run_acceptance_cycle()`：
   - `run_audit_gateway()` → `asyncio.create_subprocess_exec(fake-mad.exe)` → FakeProcess → `MadAuditGatewayResult(verdict=fail, issues=[ISSUE-001])`
   - `verdict=fail` → early return `accept_transition=None, integrate_transition=None`

3. `WorkflowOrchestrator.run_delivery_remediation()`：
   - `acquire_worker_slot()` → remediation WorkerSlotLease
   - `apply_transition(DELIVERY_RETURNED)` with DispatchCAS(dispatch_id=DSP-E2E-RECOV-001, attempt=1) under remediation lease → `review_ready → returned`
   - `release_worker_slot()` → remediation lease released
   - `apply_transition(TASK_REQUEUED)` with `lease=None`（PM-only 转换）→ `returned → ready`

### Attempt 2 — 重新派发并成功集成

4. `run_dispatch_cycle()`（attempt=2, dispatch_id=DSP-E2E-RECOV-002）：
   - 完整重复上述 dispatch 流程
   - `apply_transition(TASK_DISPATCHED)` → `ready → dispatched`
   - `apply_transition(DISPATCH_ACKNOWLEDGED)` → `dispatched → in_progress`
   - `apply_transition(DELIVERY_SUBMITTED)` → `in_progress → review_ready`

5. `run_acceptance_cycle()`：
   - `run_audit_gateway()` → FakeProcess → `MadAuditGatewayResult(verdict=pass)`
   - `acquire_worker_slot()` → review WorkerSlotLease
   - `apply_transition(DELIVERY_ACCEPTED)` with DispatchCAS(dispatch_id=DSP-E2E-RECOV-002, attempt=2) under review lease → `review_ready → accepted`
   - `release_worker_slot()`
   - `apply_transition(CHANGE_INTEGRATED)` with `lease=None` → `accepted → integrated`

## Mock 边界

| 组件 | Mock 方式 | 未 Mock |
|---|---|---|
| `asyncio.create_subprocess_exec` | 单个路由函数，按顺序分发 4 个 FakeProcess 实例 | — |
| 时钟 | FakeClock（UTC，1s tick） | — |
| `WorkflowOrchestrator` | — | ✓ 真实生产代码 |
| `ControlPlaneTransitionService.apply_transition` | — | ✓ 真实生产代码 |
| `ApprovalGate.check/require` | — | ✓ 真实生产代码 |
| `StateProvider.snapshot` | — | ✓ 真实生产代码 |
| `decode_worker_result` / `require_delivery_receipt` | — | ✓ 真实生产代码 |
| `acquire_worker_slot` / `release_worker_slot` | — | ✓ 真实生产代码 |
| `run_audit_gateway` | — | ✓ 真实生产代码 |
| `run_worker_observed` | — | ✓ 真实生产代码 |

## Identity 对照

| 属性 | Attempt 1 | Attempt 2 |
|---|---|---|
| `dispatch_id` | `DSP-E2E-RECOV-001` | `DSP-E2E-RECOV-002` |
| `attempt` | `1` | `2` |
| `outbox_message_id` | `MSG-E2E-RECOV-OUTBOX-A1` | `MSG-E2E-RECOV-OUTBOX-A2` |
| `implementation_commit` | attempt-1 提交 SHA | attempt-2 提交 SHA（不同） |
| `report_commit` | attempt-1 报告 SHA | attempt-2 报告 SHA（不同） |
| `dispatch event_id` | `EVT-DISPATCH-RECOV-A1` | `EVT-DISPATCH-RECOV-A2` |
| `delivery event_id` | `EVT-DELIVERY-RECOV-A1` | `EVT-DELIVERY-RECOV-A2` |
| `return event_id` | `EVT-RETURN-RECOV-A1` | N/A |
| `requeue event_id` | `EVT-REQUEUE-RECOV-A1` | N/A |
| `accept event_id` | `EVT-ACCEPT-RECOV-A1`（未使用） | `EVT-ACCEPT-RECOV-A2` |
| `integrate event_id` | `EVT-INTEGRATE-RECOV-A1`（未使用） | `EVT-INTEGRATE-RECOV-A2` |

所有 identity 完全独立。Attempt 2 未复用 attempt 1 的任何身份组件、event ID 或 approval evidence。

## 事件与 Guard 顺序

按 `occurred_at` 排序的完整业务事件序列：

| # | 事件类型 | 绑定的 Attempt | approval_gate guard |
|---|---|---|---|
| 1 | `TASK_DISPATCHED` | 1 | ✓ |
| 2 | `DISPATCH_ACKNOWLEDGED` | 1 | — |
| 3 | `DELIVERY_SUBMITTED` | 1 | — |
| 4 | `DELIVERY_RETURNED` | 1 | — |
| 5 | `TASK_REQUEUED` | — (PM-only) | — |
| 6 | `TASK_DISPATCHED` | 2 | ✓ |
| 7 | `DISPATCH_ACKNOWLEDGED` | 2 | — |
| 8 | `DELIVERY_SUBMITTED` | 2 | — |
| 9 | `DELIVERY_ACCEPTED` | 2 | ✓ |
| 10 | `CHANGE_INTEGRATED` | — | ✓ |

已验证不存在：
- Attempt 1 的 `DELIVERY_ACCEPTED`
- Attempt 1 的 `CHANGE_INTEGRATED`
- 重复的 `TASK_REQUEUED`
- 第三次派发
- 隐式 attempt 增长
- 复用旧 dispatch ID

## Subprocess 调用

| # | 顺序位置 | 可执行文件 | 用途 |
|---|---|---|---|
| 1 | 第 1 次 | Claude CLI (attempt 1) | Worker 交付 — exit 0, completed |
| 2 | 第 2 次 | MAD audit (attempt 1) | 审计 — verdict=fail, ISSUE-001 |
| 3 | 第 3 次 | Claude CLI (attempt 2) | Worker 交付 — exit 0, completed |
| 4 | 第 4 次 | MAD audit (attempt 2) | 审计 — verdict=pass |

- 总 subprocess 调用：恰好 4 次
- 所有调用均使用 argv tuple/list，未通过 shell 字符串执行
- 未发生真实 CLI、API、模型或网络调用

## 清理结果

| 检查项 | 结果 |
|---|---|
| Worker Slot Lease 剩余 | 0（全部释放） |
| `.tmp-*` 文件 | 0 |
| Git remote | 0 |
| cwd 改变 | 无 |
| Asyncio task 泄漏 | ≤3 tasks（无泄漏） |
| 源仓库污染 | 无（`git status --short` 无输出） |

## 已知基线失败（非回归证据）

`TestGatedSuccessPaths` 中 4 个已知基线失败 — 与本卡无关。未借机修复，未扩大范围。

## 接受标准 — 全部通过

- [x] 新增且仅新增 `tests/test_workflow_recovery_e2e.py`
- [x] 测试通过真实生产调用链覆盖失败审计、返工、重排和第二次成功集成
- [x] Attempt 从 `1` 严格增长到 `2`
- [x] 两次 dispatch ID、event ID、approval ID 和交付 SHA 相互独立
- [x] 第一次 MAD `fail` 不产生 acceptance 或 integration
- [x] `DELIVERY_RETURNED → TASK_REQUEUED` 顺序正确且只发生一次
- [x] 第二次 MAD `pass` 后任务进入 `integrated`
- [x] `StateProvider.snapshot()` 能读取最终五源一致状态
- [x] 最终 `current_dispatch is None`
- [x] 最终任务根对象不存在非法 `model_selection` 字段
- [x] 最终 acceptance 记录绑定 attempt 2 的 `reviewed_dispatch_id`
- [x] 所有 Worker Slot Lease 均已释放，剩余数量为 0
- [x] 无 `.tmp-*` 文件、Git remote、cwd 改变或源仓库污染
- [x] 无明显泄漏的 asyncio task
- [x] subprocess 调用次数、顺序和 mock 边界符合本卡要求
- [x] 指定的单条恢复 E2E 测试通过
- [x] Delivery-remediation 附近定向回归通过（30/30）
- [x] 所有 required checks 均有可复核输出
- [x] 最终只提交一份交付报告
- [x] 最终 `git status --short` 无输出
